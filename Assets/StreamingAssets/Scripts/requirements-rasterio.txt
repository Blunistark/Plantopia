# Rasterio-based requirements (easier Windows installation)
# Use this instead of GDAL if you have installation issues

numpy>=1.19.0
Pillow>=8.0.0
scipy>=1.5.0
rasterio>=1.2.0

# Installation:
# pip install -r requirements-rasterio.txt
